const initialState = {
    chartFilter : "male",
    visitsFilter : "daily",
    entryFilter : ["","","",""]
}

const filters = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default filters
