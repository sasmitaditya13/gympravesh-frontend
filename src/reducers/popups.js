const initialState = {
    chartPopup : false,
    visitsPopup : false,
    entryPopup : false
}

const filters = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default filters
